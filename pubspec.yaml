name: fl_picraft
description: "A picture craft for Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.10.8

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management — Riverpod selected per CLAUDE.md target architecture.
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Routing — declarative router selected per PRD §6 建议技术栈.
  go_router: ^14.6.2

  # Image processing — pure-Dart image library used by stitch / grid features.
  image: ^4.3.0

  # Image input on mobile / web (gallery + camera).
  image_picker: ^1.1.2

  # Fullscreen preview gallery + drag-to-dismiss — replaces self-rolled
  # InteractiveViewer + PageView + custom ScrollPhysics under task
  # `05-22-brainstorm-fullscreen-preview-extended-image` (ST1 introduces
  # the dep + a `lib/_poc/` proof; ST2/ST3/ST4 do the migration). Lock-
  # graph verified compatible per
  # `.trellis/spec/frontend/dependencies-and-platforms.md`.
  extended_image: ^10.0.1

  # Cross-platform file selection (desktop / web fallback + save dialog).
  file_picker: ^8.1.4

  # Cross-platform drag-and-drop (preferred over desktop_drop per PRD).
  super_drag_and_drop: ^0.9.0

  # Cross-platform clipboard image paste support.
  super_clipboard: ^0.9.0

  # Drag-to-reorder list of imported images.
  reorderables: ^0.6.0

  # Temporary directory access for processing pipelines.
  path_provider: ^2.1.5

  # Save images to mobile gallery — gal is actively maintained
  # (image_gallery_saver is unmaintained per PRD note).
  gal: ^2.3.0

  # Optional share sheet on save success.
  share_plus: ^10.1.2
  google_fonts: ^8.1.0

  # Web platform interop — used by the export pipeline's blob-download
  # path. Pinned to a version range compatible with Flutter SDK.
  web: ^1.1.0

  # Reads app name / version / build number at runtime (About screen +
  # showLicensePage header). Picked over hard-coded strings so a build
  # with `--build-name` / `--build-number` overrides surfaces correctly.
  package_info_plus: ^8.1.0

  # Opens external URLs (GitHub repo / issues) from the About screen.
  url_launcher: ^6.3.1

  # Pure-Dart ZIP packaging — used ONLY by the Web batch export adapter
  # via conditional import topology (`web_zip_composer_web.dart`). Non-web
  # builds compile the stub (`web_zip_composer_stub.dart`) and never pull
  # archive code into their final bundle.
  archive: ^4.0.0

  # Cross-platform path joining for the desktop directory persist adapter
  # (handles `\` vs `/` separators per platform). Already a transitive
  # dep via Flutter SDK; declared explicitly so this package self-
  # documents the direct dependency.
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Riverpod code-generation tooling.
  build_runner: ^2.4.13
  riverpod_generator: ^2.6.3

  # Mocking framework for unit tests.
  mocktail: ^1.0.4

  # FakeAsync-based time control for async provider unit tests.
  fake_async: ^1.3.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled assets shipped inside the application binary. Currently
  # limited to the About screen's app icon — sourced from
  # `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.webp` so the
  # in-app branding stays visually aligned with the platform launcher.
  assets:
    - assets/icon/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
