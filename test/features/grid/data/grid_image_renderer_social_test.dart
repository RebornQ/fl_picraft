// File retained as an empty placeholder between 05-17 Subtask B and
// Subtask C.
//
// Subtask B removed the 9-grid-social mode entirely (every grid type
// now operates on a uniform `cols / rows` crop with square cells). The
// previously-extensive coverage in this file targeted helpers that no
// longer exist (`nineGridSocialMode`, `centerImageBytes`,
// `hasCenterReplacement`, the `_composeCenterCell` branch). Per-cell
// replacement tests will be reintroduced by Subtask C against the new
// `CellOverlay` widget and a per-cell `Map<int, CellReplacement>`
// state.
void main() {}
