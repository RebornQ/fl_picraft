// File retained as an empty placeholder between 05-17 Subtask B and
// Subtask C.
//
// Subtask B stubbed [CenterCellOverlay] down to a passive
// `SizedBox`-only placeholder; the prior coverage relied on controller
// methods (`pickCenterImage`, `setCenterImage`, `setCenter*`) and state
// fields that no longer exist. Subtask C will reintroduce the
// generalized `CellOverlay` widget and the corresponding tests.
void main() {}
