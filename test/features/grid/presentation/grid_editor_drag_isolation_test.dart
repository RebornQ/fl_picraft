// File retained as an empty placeholder between 05-17 Subtask B and
// Subtask C.
//
// Subtask B removed the [CenterCellOverlay] gesture path (the overlay
// is now a passive placeholder) and the controller's `setNineGrid…` /
// `setCenter…` methods. The drag-isolation contract this file used to
// guard ("R-DRAG-05: gesture inside center cell drives the overlay,
// not the canvas drag") becomes a property of the generalized
// `CellOverlay` widget that Subtask C will reintroduce.
void main() {}
